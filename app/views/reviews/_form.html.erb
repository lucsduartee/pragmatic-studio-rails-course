<%= form_with(model: [@movie, @review]) do |f| %>
  <%= render "shared/errors", object: @review%>
  
  <%= f.label :stars %>
  <%= f.select :stars, Review::STARS, prompt: "pick onie"%>

  <%= f.label :comment %>
  <%= f.text_area :comment, placeholder: "What did you think?" %>

  <%= f.submit "Post review"%>
<% end %>
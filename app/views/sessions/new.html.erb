<h1>Sign in</h1>
<%= form_with(url: session_path) do |f| %>
  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "sign in" %>
<% end %>